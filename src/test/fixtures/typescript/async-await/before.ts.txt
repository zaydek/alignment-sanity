const fetchUser = async (id: string) => await api.getUser(id);
const fetchPosts = async (userId: string) => await api.getPosts(userId);
const fetchComments = async (postId: string) => await api.getComments(postId);
